const singleHeaders = `
<table class="meetings  {{meetingFontSize}}" data-index="{{sectionIndex}}">
    {{#if day}}
    <tr><th colspan="5" class="section-title">{{day}}</th></tr>
    {{/if}}
    <tr>{{{columns}}}</tr>
</table>
<table class="meetings {{meetingFontSize}}">
    <tbody>
        {{#each rows}}
            {{{row}}}
        {{/each}}
    </tbody>
</table>
`;