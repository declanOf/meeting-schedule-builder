const repeatColumnNamesTemplate = `
{{#if day}}
<table class="meetings {{meetingFontSize}}" data-index="{{sectionIndex}}">
    <tr class="section-title">{{day}}</tr>
</table>
{{/if}}
<table class="meetings" data-index="{{sectionIndex}}">
    <thead>
        <tr>{{{columns}}}</tr>
    </thead>
    <tbody>
        {{#each rows}}
            {{{row}}}
        {{/each}}
    </tbody>
</table>
`;