
// EXAMPLE:
// const filteredMeetings = (new MeetingFilter(meetings, Configuration.settings.filters)).filteredMeetings();

/**
 * get sections from configuration.
 * foreach section
 *     get type of section
 *         instantiate class for type of sections
 *             pass section details, meeting list
 *             build list of meetings for section
 *                 add to each meeting a section identifier
 */







/**
 * How to process these as few times as possible? There's a risk of redundancy
 */
